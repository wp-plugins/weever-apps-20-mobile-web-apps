describe('Tab Model spec', function() {
    beforeEach(function() {
        this.tabModel = new wxApp.Tab();
    });

//    it('should delete tab when deleteSubTab called', function() {
//        expect( this.tabModel.getSubTabs().length ).toEqual(0);
//        var subTab = new wxApp.SubTab();
//        this.tabModel.addSubTab( subTab );
//        expect( this.tabModel.getSubTabs().length ).toEqual(1);
//        this.tabModel.deleteSubTab( subTab );
//        expect( this.tabModel.getSubTabs().length ).toEqual(0);
//    });
});